<script>
  import { baseServiceDef, baseServiceMixin } from '{{ APP_CONFIG.server_origin }}/public/services/baseService.js';

  export function install(vue) {
    const { defineComponent } = vue;
    // const ZY = global.ZY;
    return defineComponent({
      template: '<div></div>',
      mixins: [
        baseServiceMixin,
      ],
      setup(props, ctx) {
        const config = ZY.JSON5.parse(`{{CONFIG_SOURCE_JSON5}}`);
        return baseServiceDef({ props, ctx, vue, config });
      },
    });
  }
</script>
