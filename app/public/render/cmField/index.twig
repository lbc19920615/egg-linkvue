{% import "../macros/formGroup.twig" as formGroup %}
<el-form-item :prop="getProp(pathArr)"

              :x-path="formPath"
                v-bind="ui"
              :label="getLabel()"
              :rules="rules"
>
{#{% verbatim %}{{ {% endverbatim %} getProp(pathArr) {% verbatim %} }} {% endverbatim %}#}
    {{ formGroup.formItem("value") }}
</el-form-item>
