{% import "../macros/formGroup.twig" as formGroup %}
<el-form-item :prop="prop"
              :x-path="formPath"
                v-bind="ui"
              :label="ui && ui.label ? ui.label : label"
              :rules="rules"
>
{#{% verbatim %}{{ {% endverbatim %} modelPath {% verbatim %} }} {% endverbatim %}#}
    {{ formGroup.formItem("value") }}
</el-form-item>
