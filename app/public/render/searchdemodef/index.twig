{% import "../macros/formGroup.twig" as formGroup %}
{{ source.modelName }}
<el-form :model="{{ config.source.modelName }}" :inline="true" >
    {{ formGroup.renderSet("formDef.properties", "model", 1,2, 'template') }}
</el-form>
